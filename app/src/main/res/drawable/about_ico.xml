<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 上面的圆点 -->
    <item
        android:top="0dp"
        android:left="0dp"
        android:right="0dp"
        android:bottom="40dp">
        <shape
            android:shape="oval">
            <solid android:color="@color/white"/> <!-- 点的颜色 -->
            <size
                android:width="8dp"
                android:height="8dp"/>
        </shape>
    </item>

    <!-- 中间的圆点 -->
    <item
        android:top="20dp"
        android:left="0dp"
        android:right="0dp"
        android:bottom="20dp">
        <shape
            android:shape="oval">
            <solid android:color="@color/white"/> <!-- 点的颜色 -->
            <size
                android:width="8dp"
                android:height="8dp"/>
        </shape>
    </item>

    <!-- 下面的圆点 -->
    <item
        android:top="40dp"
        android:left="0dp"
        android:right="0dp"
        android:bottom="0dp">
        <shape
            android:shape="oval">
            <solid android:color="@color/white"/> <!-- 点的颜色 -->
            <size
                android:width="8dp"
                android:height="8dp"/>
        </shape>
    </item>

</layer-list>
